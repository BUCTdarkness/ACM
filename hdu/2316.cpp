#include<iostream>
#include<cstring>
#include<algorithm>
#include<cstdio>
#include<cmath>
#include<string>
#include<map>
#include<set>
#include<vector>
#include<cstdlib>
#include<queue>
using namespace std;
#define CLR(A) memset(A,0,sizeof(A))
int *p,a[10];
int main(){
    int n;
    for(;~n?n=getchar(p=n>10?*p+=n+2,p+1:a):*p&&putchar(*p++%10+48););
    return 0;
}
